import React, { Fragment, useEffect} from 'react'

export default function Tassa() {
    return (
        <Fragment>

        <div>
          <h1>Tassa</h1>
    
    <button onClick={() => history.push('/') } >Go to home</button>  
        </div>
        </Fragment>
    )
}
